generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./grocery.db"
}

model Product {
  id    Int    @id @default(autoincrement())
  name  String
  price Int
  stock Int
}
